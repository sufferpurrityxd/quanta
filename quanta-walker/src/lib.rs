/// quanta-walker - This is a utility that 'runs' through all the files in
/// the system and checks for the presence of files that are distributed by the peer.
/// If they were deleted / moved somewhere, then quanta updates the repository
type _TODO = usize;
